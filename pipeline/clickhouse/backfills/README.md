# ClickHouse backfills

This directory is intentionally empty for the fresh-install baseline. Add a
numbered SQL backfill here only when a future schema change must transform
already-retained analytics data independently of replayable DDL migrations.
